
const env = Object.freeze({
    shared : Object.freeze({
        appTitle : 'Erik Michael Swan v2'
    }),
    client : Object.freeze({

        // events
        // event : 'EVENT'
    })
});

module.exports = env;
