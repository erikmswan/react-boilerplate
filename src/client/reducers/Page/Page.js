// import { client } from '../../../../lib/env';

const page = (state = {}, action) => {

    switch (action.type) {
        // case client.event: {
        //     const newState = {
        //         event : newVal
        //     };
        //     return Object.assign({}, state, newState);
        // }

        default: {
            return state;
        }
    }

};

export default page;
